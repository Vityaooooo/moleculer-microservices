name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push moleculer-logging-service
        uses: docker/build-push-action@v5
        with:
          context: ./moleculer-logging-service
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/moleculer-logging-service:latest

      - name: Build and push moleculer-orders-service
        uses: docker/build-push-action@v5
        with:
          context: ./moleculer-orders-service
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/moleculer-orders-service:latest

      - name: Build and push moleculer-products-service
        uses: docker/build-push-action@v5
        with:
          context: ./moleculer-products-service
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/moleculer-products-service:latest

      - name: Build and push moleculer-api-gateway
        uses: docker/build-push-action@v5
        with:
          context: ./moleculer-api-gateway
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/moleculer-api-gateway:latest